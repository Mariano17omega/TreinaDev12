<h1>Filmes Dirigidos por <%= @director.name %></h1>

<ul>
  <% @movies.each do |movie| %>
    <li><%= movie.title %></li>
  <% end %>
</ul>
 
<%= link_to 'Editar Diretor', edit_director_path(@director), class: 'btn' %>
<%= link_to 'Voltar aos Diretores', directors_path %>
